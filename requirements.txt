sqlglot>=25.0
groq>=0.11
psycopg[binary]>=3.1
psycopg-pool>=3.2
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
pytest>=8.0


# ---- Stage 1: "bring your own database" (multi-engine backend) ----
# SQLAlchemy provides one interface over Postgres/MySQL/SQLite/SQL Server/...
sqlalchemy>=2.0
# Drivers (install the one(s) you need; SQLite needs no extra driver):
pymysql>=1.1          # MySQL / MariaDB
# pyodbc>=5.0         # SQL Server (also needs a system ODBC driver) -- optional
